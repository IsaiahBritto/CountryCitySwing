/** Inline email colors — keep in sync with lib/design/tokens.ts */
import { colors } from "./tokens";

export const emailBrand = {
  gold: colors.gold,
  accent: colors.accent,
  text: colors.text,
  muted: colors.textMuted,
  background: colors.bgElevated,
} as const;
